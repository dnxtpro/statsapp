{
    "builds": [
      {
        "src": "src/main.ts",
        "use": "@vercel/angular"
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/" }
    ],
    "outputDirectory": "dist/lawea"
  }